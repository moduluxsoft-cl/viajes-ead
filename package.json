{
  "name": "viajes-ead",
  "private": true,
  "version": "dev",
  "workspaces": [
    "packages",
    "firebase/functions"
  ],
  "engines": {
    "node": ">=18 <=22"
  },
  "scripts": {
    "build:web": "npm run web:export -w @viajes-ead/app && shx rm -rf firebase/hosting/public && shx mkdir -p firebase/hosting/public && shx cp -r packages/dist/* firebase/hosting/public/",
    "build:app": "npm ci && npm run build:web && npm run build -w @viajes-ead/functions",
    "qa:serve:all": "npm run build:app && npm run emulators:start -w @viajes-ead/functions",
    "qa:start:emulators": "npm run emulators:start -w @viajes-ead/functions",
    "qa:start:view": "npm run start -w @viajes-ead/app"
  },
  "devDependencies": {
    "@babel/core": "^7.25.2",
    "@expo/metro-config": "^0.20.17",
    "@types/crypto-js": "^4.2.2",
    "@types/papaparse": "^5.5.0",
    "@types/react": "~19.0.10",
    "eslint": "^9.25.0",
    "eslint-config-expo": "~9.2.0",
    "typescript": "~5.8.3",
    "shx": "^0.4.0"
  },
  "overrides": {
    "pretty-format": "^29.7.0"
  }
}
