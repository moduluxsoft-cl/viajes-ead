{
  "name": "viajes-ead",
  "private": true,
  "workspaces": [
    "packages/*",
    "firebase/functions"
  ],
  "engines": {
    "node": ">=18 <=22"
  },
  "scripts": {
    "build:web": "npm run web:export -w @viajes-ead/app && rm -rf firebase/hosting/public && mkdir -p firebase/hosting/public && cp -r packages/app/dist/* firebase/hosting/public/",
    "web:export": "npx --yes expo export --platform web --output-dir dist",
    "qa:prepare": "npm ci && npm ci -w @viajes-ead/functions && npm run build:web && npm run build -w @viajes-ead/functions",
    "qa:serve:hosting": "npm run build:web && (cd firebase && npx firebase emulators:start --only hosting --project default --config firebase.json)",
    "qa:serve:functions": "npm run build -w @viajes-ead/functions && (cd firebase && npx firebase emulators:start --only functions --project default --config firebase.json)",
    "qa:serve:all": "npm run qa:prepare && (cd firebase && npx firebase emulators:start --project default --config firebase.json)",
    "deploy:hosting:prod": "npm run build:web && (cd firebase && npx firebase deploy --only hosting --project default --config firebase.json)",
    "functions:install": "npm ci -w @viajes-ead/functions",
    "functions:build": "npm run build -w @viajes-ead/functions",
    "functions:typecheck": "npm run typecheck -w @viajes-ead/functions"
  },
  "devDependencies": {
    "typescript": "5.7.3",
    "eslint": "8.9.0"
  }
}